import {describe,expect,it}from'vitest';import{render,screen,fireEvent}from'@testing-library/react';import App from'./App';
describe('Daily Lock-In UI',()=>{it('logs two shower stages',()=>{localStorage.clear();render(<App/>);const button=screen.getByRole('button',{name:/refresh 1 of 2/i});fireEvent.click(button);expect(screen.getByText('1 of 2 resets complete')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/refresh 2 of 2/i}));expect(screen.getByText('2 of 2 resets complete')).toBeInTheDocument()});it('gates Lock In until prerequisites are complete',()=>{localStorage.clear();render(<App/>);expect(screen.getByRole('button',{name:/complete prerequisites/i})).toBeDisabled();fireEvent.click(screen.getByRole('button',{name:/refresh 1 of 2/i}));fireEvent.click(screen.getByRole('button',{name:/work clothes/i}));fireEvent.click(screen.getByRole('button',{name:/environment reset/i}));expect(screen.getByRole('button',{name:/ignite focus/i})).toBeEnabled()})});